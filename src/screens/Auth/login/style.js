import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 20,
  },
  stretch: {
    width: 70,
    height: 90,
    resizeMode: 'stretch',
    alignSelf: 'center',
  },
});
export default styles;
