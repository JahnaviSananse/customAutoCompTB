import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  text: {fontSize: 20},
  view: {
    height: 28,
    marginLeft: 30,
    marginTop: 10,
    width: '85%',
  },
});
export default styles;
