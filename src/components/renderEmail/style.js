import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  text: {fontSize: 13},
  view: {
    height: 28,
    marginLeft: 30,
    marginTop: 10,
    width: '40%',
    // backgroundColor: 'red',
    borderRadius: 10,
    // position: 'absolute',
    // position: 'relative',
  },
});
export default styles;
